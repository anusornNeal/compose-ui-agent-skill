# Deprecated Designer Mode

This root document is retained only as a migration pointer. Use the directly invokable skill at `skills/compose-ui-designer` and call `$compose-ui-designer`.

The Designer -> Critic -> explicit approval workflow now lives entirely inside that skill.
