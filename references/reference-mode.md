# Deprecated Reference Mode

This root document is retained only as a migration pointer. Use the directly invokable skill at `skills/compose-ui-reference` and call `$compose-ui-reference`.

Figma extraction belongs to the official `$figma-implement-design` skill. Screenshot normalization is documented inside the reference skill.
