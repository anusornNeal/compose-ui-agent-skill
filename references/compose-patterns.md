# Deprecated Compose Patterns

This root document is a migration pointer. The maintained Compose convention resolver lives at `skills/compose-ui-delivery/references/compose-patterns.md` and is used by `$compose-ui-delivery`.
